__pycache__/
*.pyc
build/
data/cache/
site/map/report.html

# Regenerated by scripts/gzip_data.py, and only useful to serve.py -- a static
# host compresses for itself. 134 MB of pure redundancy if committed.
*.json.gz

# 620 MB of raw per-municipality cadastre downloads, re-fetchable with
# scripts/fetch_all_parcels.py. What matters is what build_all_tiles.py makes
# of them, and that is committed as site/map/data/tiles_all/.
data/all_parcels/
